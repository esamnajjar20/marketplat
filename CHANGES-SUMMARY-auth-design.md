# ملخص التعديلات — سوق غزة (تصميم مجموعة `(auth)`)

ملف واحد فقط: `frontend/app/(auth)/layout.tsx` — انسخه ليحل محل الملف
الموجود في مشروعك.

## ما تغيّر

التقرير لم يسجّل أي خلل تصميمي صريح في `(auth)` (فقط الملاحظة المعمارية
حول تكرار `min-h-screen` التي عولجت في الدفعة السابقة)، لكن بمراجعة
الصفحات بصرياً ظهرت فجوة حقيقية:

**1) لوحة الهوية بلا نسيج بصري:** لوحة `bg-primary` الجانبية (الاقتباس +
الشعار) كانت تعبئة لونية مسطحة، بخلاف كل مكان آخر يستخدم نفس اللون
(Hero الرئيسية، شريط `/search`) الذي يحمل نسيج القطع القطري الخفيف
(`repeating-linear-gradient`) كإشارة هوية متسقة. أُضيف نفس النسيج هنا.

**2) الهوية البصرية غائبة تماماً على الموبايل:** اللوحة الجانبية
مخفية بالكامل تحت `lg` (`hidden lg:flex`) — أي أن معظم الزوار (شاشات
الهاتف) لا يرون لونها أو اقتباسها إطلاقاً، وكان البديل الوحيد شعاراً
عادياً بلا أي لون. أُضيف شريط علوي بلون `bg-primary` (بنفس النسيج،
بارتفاع مضغوط) يظهر فقط تحت `lg` كبديل مكافئ — نفس منطق "الشريط
العلوي" المستخدَم في `/search`، بدل أن تبقى شاشة الهاتف بطاقة بيضاء بلا
أي إشارة هوية.

## قائمة الملفات في هذه الحزمة

```
frontend/app/(auth)/layout.tsx
```

لا تغيير على أي من الصفحات الأربع أو مكوّنات النماذج — التعديل محصور
بالكامل في الـ Layout المشترك.
