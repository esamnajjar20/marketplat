/**
 * Shared Arabic UI strings (buttons, empty states, loading labels, etc.)
 * not tied to a specific backend error code.
 *
 * Deliberately empty for now — the current pass of work is scoped to
 * error-code translation (errors.ts). Existing inline Arabic toast/UI
 * strings throughout the app are correct as-is and weren't moved here
 * to avoid unrelated churn; add entries here as shared strings are
 * identified and consolidated.
 */
export {};
