/**
 * Button — re-exports the shadcn/ui Button with no modifications.
 * Centralise the import so future changes only touch this file.
 */
export { Button, buttonVariants } from '@/components/ui/button';
export type { ButtonProps }       from '@/components/ui/button';
